# Getting Started

1. Install dependencies with `pnpm install`.
2. Run development services with `pnpm dev`.
3. Open the web app at `http://localhost:3000`.
4. Follow the onboarding checklist to connect accounts and render a sample clip.

![Screenshot placeholder](./screenshot.png)
